# Dependencies
node_modules/
.pnpm-store/

# Build outputs
.next/
out/
dist/
build/
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local

# Volumes (persistent data, never commit)
data_public/
data_db/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Loop / agent scratch notes (running task log, never commit)
loop-notes.md

# Editor
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code (local agent config / session data)
.claude/

# Electron build artifacts
electron/dist/
electron/out/
electron/baked.json

# Native VR bridge staging (regenerated by build:native). The CMake build/ dir
# is already covered by the global build/ rule above; the vendored OpenVR SDK
# under native/vr-bridge/vendor IS committed.
electron/resources/vr/
